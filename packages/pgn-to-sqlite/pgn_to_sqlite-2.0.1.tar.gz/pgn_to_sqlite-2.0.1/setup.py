#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['pgn_to_sqlite']

package_data = \
{'': ['*']}

install_requires = \
['click', 'requests', 'berserk']

entry_points = \
{'console_scripts': ['pgn-to-sqlite = pgn_to_sqlite.cli:cli']}

setup(name='pgn_to_sqlite',
      version='2.0.1',
      description='Fetch your games from chess.com and lichess.org and add them to a sqlite database',
      author='Ricky White',
      author_email='ricky@whitelionmedia.com',
      url='https://github.com/EndlessTrax/pgn-to-sqlite',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.6',
     )
