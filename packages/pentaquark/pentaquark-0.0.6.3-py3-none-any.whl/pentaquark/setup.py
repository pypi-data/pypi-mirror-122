# from pentaquark.properties import RelationshipPropertyOut
# from pentaquark.registry import node_registry


def setup(paths=None, klasses=None):
    # # this is where we can create related fields?
    # for label, model in node_registry.items():
    #     print(label, model)
    #     if label == "Movie":
    #         setattr(
    #             model,
    #             "test_prop",
    #             RelationshipPropertyOut("HAS_TYPE", "Person")
    #         )
    pass
