[tool.poetry]
name = "tehut"
version = "0.1.0.2"
description = ""
authors = ["Lennart Keller <lennart.keller@googlemail.com>"]

[tool.poetry.dependencies]
python = "^3.8"
requests = "^2.26.0"
more-itertools = "^8.10.0"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
