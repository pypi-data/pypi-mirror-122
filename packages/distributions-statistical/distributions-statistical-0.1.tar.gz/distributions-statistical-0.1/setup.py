from setuptools import setup

setup(name='distributions-statistical',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions-statistical'],
      zip_safe=False)
