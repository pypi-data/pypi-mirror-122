from setuptools import setup

setup(name='binomial-and-gaussian',
      version='0.1',
      description='Gaussian and Binmialdistributions',
      packages=['binomial-and-gaussian'],
      zip_safe=False)
