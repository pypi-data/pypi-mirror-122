from setuptools import setup

setup(name='mathematical_distributions',
      version='0.1',
      description='Binomial and Gaussian distributions',
      packages=['mathematical_distributions'],
      zip_safe=False)
