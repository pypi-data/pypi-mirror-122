
from setuptools import setup

setup(
    name='defaultmod',
    version='1.0.1',
    description='All the default modules imported into one module.',
    long_description='import defaultmod as i; i.re',
    packages=["defaultmod", "defaultmodwerrors"]
    )

