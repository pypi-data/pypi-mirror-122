from setuptools import setup

setup(name='distributions_simple',
      version='0.1',
      description='Gaussian distributions, Binomial distributions',
      packages=['distributions_simple'],
      zip_safe=False)